# Managed files notice

Files recorded as `constructor` in `.project-constructor/state.json` are generated and maintained by
Project Engineering OS under the MIT license included with the package. Files recorded as `project`,
`human-overlay`, or `external-openspec` remain controlled by the consumer or their declared owner.

Installing or upgrading Project Engineering OS does not select, create, replace, or change the main
`LICENSE` of the consumer repository. The consumer must choose its own product license.
